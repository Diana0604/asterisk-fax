;Local/201@devices[/n]

;[my_gosub_routine]
;exten => s,1,NoOp(ARG1=${ARG1} ARG2=${ARG2})
;same => n,Playback(hello)
;same => n,Playback(hello)
;same => n,Playback(hello)
;same => n,Playback(hello)
;same => n,Playback(hello)
;same => n,Playback(hello)
;same => n,Playback(hello)
;same => n,Return()

;[from-internal]
;exten => 201,1,Verbose(1, test)
;same=>n,Dial(PJSIP/1000,,U(my_gosub_routine^my_gosub_arg1^my_gosub_arg2))
;same=>n,Gosub(hello,hello,1)
;same=>n(hangup),Hangup()

[from-internal-custom]
exten=>3456,1,Verbose(1, 3456 called)
same=>n,Answer()
same=>n,Wait(1)
same=>n,Verbose(1, recording)
same=>n,MixMonitor(/fax/sounds/phone/output/name.wav)
same =>n,Wait(10)
same=>n,Verbose(1, stopping recording)
same=>n,StopMixMonitor()
same=>n,Playback(/fax/sounds/phone/01_nice_to_meet_you)
same=>n,Verbose(1, continue)
same=>n,Playback(/fax/sounds/phone/output/name)

[hello]
exten=>hello,1,Verbose(1, hello world call)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)



exten=>1,1,Verbose(1, one has been pressed)
same=>n,Set(DB(DRHA/show)=started)
same=>n,Answer()
same=>n,Wait(1)
same=>n,Playback(/fax/sounds/phone/00_hi_name)
same=>n,Verbose(1, recording)
same=>n,MixMonitor(/fax/sounds/phone/output/name.wav)
same =>n,Wait(3)
same=>n,Verbose(1, stopping recording)
same=>n,StopMixMonitor()
same=>n,Gosub(name_check,check,1)

[name_check]
exten=>check,1,Verbose(name check reached)
same=>n,Playback(/fax/sounds/phone/01_YourName)
same=>n,Verbose(1, continue)
same=>n,Playback(/fax/sounds/phone/output/name)
same=>n,Background(/fax/sounds/phone/02_Record_again)
same=>n,Background(/fax/sounds/phone/02_Record_again)
same=>n,Background(/fax/sounds/phone/02_record_again)
same=>n,Hangup()

exten=>2,1,Verbose(correct name)
same=>n,Playback(/fax/sounds/phone/03_Lovely_name)
same=>n,Playback(/fax/sounds/phone/output/name)
same=>n,Hangup()

exten=>1,1,Verbose(re record)
same=>n,Answer()
same=>n,Wait(1)
same=>n,Playback(/fax/sounds/phone/04_repeat_name)
same=>n,MixMonitor(/fax/sounds/phone/output/name.wav)
same=>n,Wait(3)
same=>n,StopMixMonitor()
same=>n,Gosub(name_check,check,1)
same=>n,Hangup()

[infinitescroll]
exten=>infinitescroll,1,Verbose(1, inf scroll call)
same => n,SendFax(/fax/tifs/infinite_scroll.tif)
;same => n,SendFax(/fax/tifs/tester.tif)
same=>n,Hangup()

[from-internal-custom]
exten=>000,1,Verbose(1, test hello)
same=>n,Gosub(hello,hello,1)
same=>n,Hangup()

exten=>004,1,Verbose(1, test questionnaire)
same=>n,Gosub(questionnaire,q1,1)
same=>n,Hangup()

exten=>006,1,Verbose(1, test questionnaire)
same=>n,Gosub(questionnaire,q2,1)
same=>n,Hangup()

exten=>008,1,Verbose(1, test questionnaire)
same=>n,Gosub(questionnaire,q3,1)
same=>n,Hangup()

[questionnaire]

exten=>redirect-to-question,1,Verbose(1,redirect to question)
same=>n,GosubIf($[${DB(DRHA/questionnaire)} = 0]?q1,1)
same=>n,GosubIf($[${DB(DRHA/questionnaire)} = 1]?q2,1)
same=>n,GosubIf($[${DB(DRHA/questionnaire)} = 2]?q3,1)
same=>n,GosubIf($[${DB(DRHA/questionnaire)} = 3]?finished,1)
same=>n,GosubIf($[${DB(DRHA/questionnaire)} = 11]?finished,1)

;redirect to other calls from internal custom
exten=>1,1,Verbose(1, redirect for press 1)
same=>n,Set(DB(DRHA/answer)=1)
same=>n,Gosub(redirect-to-question,1)
same=>n,Hangup()

exten=>2,1,Verbose(1, redirect for press 2)
same=>n,Set(DB(DRHA/answer)=2)
same=>n,Gosub(redirect-to-question,1)
same=>n,Hangup()

exten=>3,1,Verbose(1, redirect for press 3)
same=>n,Set(DB(DRHA/answer)=3)
same=>n,Gosub(redirect-to-question,1)
same=>n,Hangup()

exten=>4,1,Verbose(1, redirect for press 4)
same=>n,Set(DB(DRHA/answer)=4)
same=>n,Gosub(redirect-to-question,1)
same=>n,Hangup()

exten=>5,1,Verbose(1, redirect for press 5)
same=>n,Set(DB(DRHA/answer)=5)
same=>n,Gosub(redirect-to-question,1)
same=>n,Hangup()

exten=>6,1,Verbose(1, redirect for press 6)
same=>n,Set(DB(DRHA/answer)=6)
same=>n,Gosub(redirect-to-question,1)
same=>n,Hangup()

exten=>7,1,Verbose(1, redirect for press 7)
same=>n,Set(DB(DRHA/answer)=7)
same=>n,Gosub(redirect-to-question,1)
same=>n,Hangup()

exten=>8,1,Verbose(1, redirect for press 8)
same=>n,Set(DB(DRHA/answer)=8)
same=>n,Gosub(redirect-to-question,1)
same=>n,Hangup()

exten=>9,1,Verbose(1, redirect for press 9)
same=>n,Set(DB(DRHA/answer)=9)
same=>n,Gosub(redirect-to-question,1)
same=>n,Hangup()

exten=>q1,1,Verbose(1, question one -> is the world too fast)
same =>n,Set(DB(DRHA/questionnaire)=1)
same=>n,Verbose(1, Wait is finished)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()

exten=>q2,1,Verbose(1, question two -> servers)
same =>n,Set(DB(DRHA/questionnaire)=2)
same=>n,Verbose(1, Wait is finished)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()

exten=>q3,1,Verbose(1, question three -> servers)
same =>n,Set(DB(DRHA/questionnaire)=3)
same=>n,Verbose(1, Wait is finished)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()

exten=>q4,1,Verbose(1, question four -> servers)
same =>n,Set(DB(DRHA/questionnaire)=4)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()


exten=>q5,1,Verbose(1, question 5 -> servers)
same =>n,Set(DB(DRHA/questionnaire)=5)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()

exten=>q6,1,Verbose(1, question 6 -> servers)
same =>n,Set(DB(DRHA/questionnaire)=6)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()

exten=>q7,1,Verbose(1, question 7 -> servers)
same =>n,Set(DB(DRHA/questionnaire)=7)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()

exten=>q8,1,Verbose(1, question 8 -> servers)
same =>n,Set(DB(DRHA/questionnaire)=8)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()

exten=>q9,1,Verbose(1, question 9 -> servers)
same =>n,Set(DB(DRHA/questionnaire)=9)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()

exten=>q10,1,Verbose(1, question 10 -> servers)
same =>n,Set(DB(DRHA/questionnaire)=10)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()

exten=>q11,1,Verbose(1, question 11 -> servers)
same =>n,Set(DB(DRHA/questionnaire)=11)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()


exten=>finished,1,Verbose(1, questionnaire is finished)
same=>n,Set(DB(DRHA/questionnaire)=finished)
same=>n,Background(/fax/sounds/phone/silence)
same=>n,Hangup()


;TESTER
[tester]
exten =>send_fax,1,Verbose(1, tester call)
same => n,SendFax(/fax/tifs/output/${EPOCH}.tif)
same => n,Hangup()

exten=>ask_name,1,Verbose(1, tester call - ask name)
same=>n,Verbose(1, recording)
same=>n,MixMonitor(/fax/sounds/phone/output/name.wav)
same =>n,Wait(10)
same=>n,Verbose(1, stopping recording)
same=>n,StopMixMonitor()
same=>n,Playback(/fax/sounds/phone/01_nice_to_meet_you)
same=>n,Verbose(1, continue)
same=>n,Playback(/fax/sounds/phone/output/name)

[from-internal-custom]
exten=>1,1,Verbose(1, custom call)
same => n,Playback(/fax/sounds/phone/00_phone_rings)
same=>n,Hangup()

exten=>663960741,1,Verbose(1, custom fax)
same=>n,ReceiveFax(/fax/tifs/output/test.tif)
same=>n,Hangup()

exten=>314159265,1,Verbose(1, cosncious social media)
same=>n,ReceiveFax(/fax/tifs/output/post.tif)
same=>n,Set(DB(DRHA/faxIncoming)=received)
same=>n,Hangup()

exten=>_00033XX,1,Verbose(1, cosncious social media)
same=>n,Playback(/fax/sounds/phone/sorry)
same=>n,Hangup()

exten=>_00033X,1,Verbose(1, cosncious social media)
same=>n,Playback(/fax/sounds/phone/sorry)
same=>n,Hangup()

[credits]
exten=>credits,1,Verbose(1, credits)
same=>n,SendFax(/fax/tifs/credits.tif)
same=>n,Hangup()
