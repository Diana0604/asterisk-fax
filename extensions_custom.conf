;HEALTHCHECK COMS
[healthcheck]
exten => 000,1,Verbose(1, call zero)
same => n,Answer()
same => n,Playback(faxmachine/healthcheck1)
same => n,Hangup()

[from-internal-custom]
exten => 123,1,Verbose(1, healthline called received)
same => n,Answer()
same => n,Playback(faxmachine/healthcheck2)
same => n,System(sendemail -f diana.vallverdu@gmail.com -t diana.vallverdu@gmail.com -m 'new call' -u 'Healthline call has been made' -xu diana.vallverdu@gmail.com -xp fcnxcntclkxrrxvd -s smtp.gmail.com)
same => n,Hangup()

[from-internal-custom]
exten => 456,1,Verbose(3, sending fax)
same => n,Set(FAXDEST=/tmp)
same => n,Set(tempfax=${STRFTIME(,,%C%y%m%d%H%M)})
same => n,ReceiveFax(${FAXDEST}/${tempfax}.tif,d)
same => n,Verbose(3, fax received)
same => n,System(sendemail -f diana.vallverdu@gmail.com -t diana.vallverdu@gmail.com -m 'new fax' -u 'Healthline fax has been made' -a ${FAXDEST}/${tempfax}.tif -xu diana.vallverdu@gmail.com -xp fcnxcntclkxrrxvd -s smtp.gmail.com)


[healthcheck]
exten => 003,1,Answer()
same => n,SendFax(/fax/tifs/healthcheck.tif)
same => n,Hangup()

;----------------------- TBL COMS PART 1 -------------------------------
;general
[from-internal-custom]
exten => 900,1,Verbose(1, receiving fax)
same => n,Set(FAXDEST=/tmp)
same => n,Set(tempfax=${STRFTIME(,,%C%y%m%d%H%M)})
same => n,ReceiveFax(${FAXDEST}/${tempfax}.tif,d)
same => n,Verbose(3, fax received)
same => n,System(sendemail -f diana.vallverdu@gmail.com -t diana.vallverdu@gmail.com -m 'NEW TBL FAX' -u 'Tbl fax' -xu diana.vallverdu@gmail.com -a ${FAXDEST}/${tempfax}.tif -xp fcnxcntclkxrrxvd -s smtp.gmail.com)
;what we send
[tbl]
exten => 01helloworld,1,Answer()
same => n,SendFax(/fax/tifs/01-helloworld.tif)
same => n,Hangup()

[tbl]
exten => 02trust1,1,Answer()
same => n,SendFax(/fax/tifs/02-trust1.tif)
same => n,Hangup()

[from-internal-custom]
exten => 126,1,Answer()
same => n,Playback(faxmachine/clearance1)
same => n,System(sendemail -f diana.vallverdu@gmail.com -t diana.vallverdu@gmail.com -m 'TRUST EX 1 COMPLETED' -u 'Trust ex 1 completed' -xu diana.vallverdu@gmail.com -xp fcnxcntclkxrrxvd -s smtp.gmail.com)
same => n,Hangup()

[tbl]
exten => 02trust2,1,Answer()
same => n,SendFax(/fax/tifs/02-trust2.tif)
same => n,Hangup()

[from-internal-custom]
exten => 1999,1,Answer()
same => n,Playback(faxmachine/clearance2)
same => n,System(sendemail -f diana.vallverdu@gmail.com -t diana.vallverdu@gmail.com -m 'TRUST EX 2 COMPLETED' -u 'Trust ex 2 completed' -xu diana.vallverdu@gmail.com -xp fcnxcntclkxrrxvd -s smtp.gmail.com)
same => n,Hangup()

[tbl]
exten => 02trust3,1,Answer()
same => n,SendFax(/fax/tifs/02-trust3.tif)
same => n,Hangup()

[tbl]
exten => 03antenna,1,Answer()
same => n,SendFax(/fax/tifs/03-antenna.tif)
same => n,Hangup()

[from-internal-custom]
exten => 555,1,Answer()
same => n,Playback(faxmachine/tbl)
same => n,System(sendemail -f diana.vallverdu@gmail.com -t diana.vallverdu@gmail.com -m 'antenna completed' -u 'ANTENNA COMPLETED' -xu diana.vallverdu@gmail.com -xp fcnxcntclkxrrxvd -s smtp.gmail.com)
same => n,Hangup()

[tbl]
exten => 04spies,1,Answer()
same => n,SendFax(/fax/tifs/04-spies.tif)
same => n,Hangup()

[from-internal-custom]
exten => 42,1,Answer()
same => n,Playback(faxmachine/answers)
same => n,System(sendemail -f diana.vallverdu@gmail.com -t diana.vallverdu@gmail.com -m 'SPIES COMPLETED' -u 'SPIES COMPLETED' -xu diana.vallverdu@gmail.com -xp fcnxcntclkxrrxvd -s smtp.gmail.com)
same => n,Hangup()

[tbl]
exten => 05mission,1,Answer()
same => n,SendFax(/fax/tifs/05-mission.tif)
same => n,Hangup()


;CONSIGNIA
[consignia]
exten => 06-consignia,1,Answer()
same => n,SendFax(/fax/tifs/06-consignia.tif)
same => n,Hangup()

;TBL COMS PART 2 (LAST STEPS)
[laststeps]
exten => 07-laststeps1,1,Answer()
same => n,SendFax(/fax/tifs/07-laststeps1.tif)
same => n,Hangup()

[laststeps]
exten => 07-laststeps2,1,Answer()
same => n,SendFax(/fax/tifs/07-laststeps2.tif)
same => n,Hangup()

[laststeps]
exten => 07-laststeps3,1,Answer()
same => n,SendFax(/fax/tifs/07-laststeps3.tif)
same => n,Hangup()

[laststeps]
exten => 07-laststeps4,1,Answer()
same => n,SendFax(/fax/tifs/07-laststeps4.tif)
same => n,Hangup()

[laststeps]
exten => 07-laststeps5,1,Answer()
same => n,SendFax(/fax/tifs/07-laststeps5.tif)
same => n,Hangup()









;THESE THRE HAVE TO BE SENT IN A ROW















[from-internal-custom]
exten => 0,1,Answer()
same => n,Set(FAXSENT=0,g)
;same => n,Playback(custom/03)
;same => n,System(cp /root/04.call /var/spool/asterisk/outgoing/)
same => n,Hangup()

[from-internal-custom]
exten => 156,1,Answer()
;exten => n,Set(FAXSENT=1)
;same => n,Playback(custom/03)
;same => n,System(cp /root/04.call /var/spool/asterisk/outgoing/)
same => n,Hangup()